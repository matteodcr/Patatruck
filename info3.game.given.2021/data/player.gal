Player(Init){
  * (Init):
  | !(Key(FU) & True) ? 24%Move(N) /  Move : ()
  | Key(FD) ? Move(S) :(Init)
  | Key(FL) ? Move(O) :(Init)
  | Key(FR) ? Move(E) :(Init)
  * (Test):
  | True ? Pop : (Init)
}
